# ADVANCED-DATA-STRUCTURE
Linear Probing: Checks next available slot sequentially   Quadratic Probing: Uses 𝑖 jumps to reduce clustering   Double Hashing: Uses a second hash function for step size   Separate Chaining: Uses linked lists at each index
